background(41,99,183);

void player(){
  background(41,99,183);
  fill(255,24,24);
  ellipse(mouseX,mouseY,10,10);
}

void projectiles(){
  int x = round(random(320));
	int y = 0;
  ellipse(x,y,6,6);
  

}

void main(){
  
  animate(player,1);
  animate(projectiles);
}
